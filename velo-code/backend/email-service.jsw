// Backend email service for contact form processing
import { sendEmail } from 'wix-crm-backend';

export async function sendContactForm(data) {
  return sendEmail({
    to: "you@example.com",
    subject: "New Contact Form Submission",
    body: `Name: ${data.name}, Message: ${data.message}`
  });
}